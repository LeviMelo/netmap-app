// frontend/src/types/react-cytoscapejs.d.ts

// Simplest possible declaration to satisfy the import system
declare module 'react-cytoscapejs';

// If this works, we can progressively add back the detailed props from Attempt 2
// If even this fails after restarting TS server, the issue is likely tsconfig setup.